# Frozen docs data: LibreYOLO 1.5.0

`registry.json`, `nav.json` and `upstream/` are copies of `src/data/docs/` taken
from the website's `main` branch at commit `19c1ca6`. They feed the archived
docs tree in `content/archive/docs/v1.5.0/`, served at `/docs/v1.5.0/...`.

**Frozen. Never edit these files.** Do not regenerate them with the registry
builder. The only permitted change is replacing the whole snapshot by copying
again from `main` at release time.
